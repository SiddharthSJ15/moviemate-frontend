# Example environment variables for Movie Hub
# Copy this file to `.env` and replace the placeholder with your OMDb API key.
# Do NOT commit your real `.env` to version control.

VITE_OMDB_API_KEY=your_omdb_api_key_here
